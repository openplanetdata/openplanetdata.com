---
title: OpenStreetMap Continents
description: Daily OpenStreetMap continent extracts in PBF, GOL, GOB, and GeoParquet formats.
# Hidden while the dataset is being fixed: not linked from the sidebar or the
# OpenStreetMap page, and kept out of site search, the sitemap and search
# engines. Drop this block once the data is back.
pagefind: false
head:
  - tag: meta
    attrs:
      name: robots
      content: noindex
---

import DatasetFiles from '../../../../components/DatasetFiles.astro';

Continent-level extracts of the OpenStreetMap planet, rebuilt daily. Pick a
format, then download the continent you need.

<DatasetFiles category="openstreetmap" subcategory="continents" defaultFormat="pbf" showVersion />
