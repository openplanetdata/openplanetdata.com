---
title: Planet Boundaries
description: Planet-wide continent, country, and region boundary files in GeoJSON, GeoPackage, and GeoParquet formats.
---

import DatasetFiles from '../../../../components/DatasetFiles.astro';

Every boundary of a given level in a single file, so a whole layer can be loaded
in one download instead of stitching per-entity files together. Pick a format,
then the level of detail you need.

<DatasetFiles category="boundaries" entity="planet" version="v2" />
